<h1>Stupid Coaching</h1>

<div class="callout"><%= @answer %><div class=italic><%= '-You' %></div></div>

<% if @answer == 'I am going to work' %>
  <div class="callout-b"><%= 'Great!' %><div class=italic><%= '-Your Coach' %></div></div>
<% elsif @answer.include? '?' %>
  <div class="callout-b"><%= 'Silly question, get dressed and go to work!' %><div class=italic><%= '-Your Coach' %></div></div>
<% else %>
    <div class="callout-b"><%= "I don't care, get dressed and go to work!" %><div class=italic><%= '-Your Coach' %></div></div>
<% end %>

<p><%= link_to "Ask a new question", ask_path %></p>
